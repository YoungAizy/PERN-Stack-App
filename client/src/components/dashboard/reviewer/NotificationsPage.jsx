import React from 'react'
import Notifications from '../Notifications'

export default function NotificationsPage({cardWrapperClasses}) {
  return (
    <Notifications wrapperClasses={cardWrapperClasses} />
  )
}
